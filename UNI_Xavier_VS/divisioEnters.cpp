#include <iostream>

using namespace std;

int divisio(int dividend, int divisor){
    
    public;
    bool hacible = 1;
    
    hacible = (divisor != 0);
    
    res = dividend/divisor;
    
    return hacible;
}